export interface WaBenefit {
  title: string;
  description: string;
  badge: string;
}

export interface WaTemplateSample {
  title: string;
  event: string;
  messageText: string;
  recipient: string;
}
